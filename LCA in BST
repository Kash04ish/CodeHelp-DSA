// Function signature
// Node* LCAinBST(Node* root, Node* p, Node* q) { // p and q are the nodes
//     while (root != NULL) {
//         if (root->data < p->data && root->data < q->data) {
//             root = root->right; // Both p and q are in the right subtree
//         } else if (root->data > p->data && root->data > q->data) {
//             root = root->left; // Both p and q are in the left subtree
//         } else {
//             return root; // Found the LCA
//         }
//     }
//     return NULL; // Should not reach here if p and q are in the tree
// }
